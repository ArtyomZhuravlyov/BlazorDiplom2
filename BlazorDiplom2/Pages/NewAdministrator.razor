@inject RoleManager<IdentityRole> _RoleManager;
@inject UserManager<AspNetUsers> _userManager;
@inject DB _db;

<h3>Administrator</h3>

  @if (users == null)
    {
        <p><em>Loading...</em></p>
    }
    else
    {
        @foreach (var user in users)
        {
            <div>
                <MyUser User="user" />
            </div>
        }
    }

@code {

     List<AspNetUsers> users;

   // protected override void OnInitialized() => users = _db.Ad//_userManager.Users.Where(x=>x.).ToList();
}
